<%= form_with(model: kanji, local: true) do |form| %>
  <% if kanji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kanji.errors.count, "error") %> prohibited this kanji from being saved:</h2>

      <ul>
        <% kanji.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :kanji %>
    <%= form.text_field :kanji %>
  </div>

  <div class="field">
    <%= form.label :kun %>
    <%= form.text_field :kun %>
  </div>

  <div class="field">
    <%= form.label :on %>
    <%= form.text_field :on %>
  </div>
  
  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %><br>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
