<%= form_with(model: kanji_review, local: true) do |form| %>
  <% if kanji_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kanji_review.errors.count, "error") %> prohibited this kanji_review from being saved:</h2>

      <ul>
        <% kanji_review.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :kanji_id %>
    <%= form.number_field :kanji_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>